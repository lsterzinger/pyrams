[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta" 

[project]
name = "pyrams"
version = "0.8.6"
dependencies = [
    'metpy',
    'pandas',
    'numpy',
    'tqdm',
    'xarray',
    'matplotlib',
    'shapely',
    'netCDF4',
]
